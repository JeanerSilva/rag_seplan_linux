def normalize_query(text: str) -> str:
    """
    Normaliza sinônimos comuns para melhorar recuperação semântica.
    """
    #text = text.lower()
    #text = text.replace("pecuária", "agropecuária")
    #text = text.replace("desenvolvimento sustentável", "sustentabilidade")
    return text
